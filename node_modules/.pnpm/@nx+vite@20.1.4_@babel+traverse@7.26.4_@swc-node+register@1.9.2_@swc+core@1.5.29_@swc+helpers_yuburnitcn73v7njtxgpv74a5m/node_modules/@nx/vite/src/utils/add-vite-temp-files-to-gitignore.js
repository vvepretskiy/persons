"use strict";
Object.defineProperty(exports, "addViteTempFilesToGitIgnore", {
    enumerable: true,
    get: function() {
        return addViteTempFilesToGitIgnore;
    }
});
const _devkit = require("@nx/devkit");
function addViteTempFilesToGitIgnore(tree) {
    let newGitIgnoreContents = `vite.config.*.timestamp*`;
    if (tree.exists('.gitignore')) {
        const gitIgnoreContents = tree.read('.gitignore', 'utf-8');
        if (!gitIgnoreContents.includes(newGitIgnoreContents)) {
            newGitIgnoreContents = (0, _devkit.stripIndents)`${gitIgnoreContents}
        ${newGitIgnoreContents}`;
            tree.write('.gitignore', newGitIgnoreContents);
        }
    } else {
        tree.write('.gitignore', newGitIgnoreContents);
    }
}

//# sourceMappingURL=add-vite-temp-files-to-gitignore.js.map